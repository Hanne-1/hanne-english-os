# V2.27.1 Speaking Brief 與 Voice 驗收

## 使用方式

1. 在 Speaking 頁選擇教材。
2. 按「準備口說內容」並複製 Brief。
3. 貼到 Hanne's English OS ChatGPT Project，再開啟語音模式。
4. Voice 結束後，請 ChatGPT 依 Brief 產生 `SPEAKING_REPORT` JSON。
5. 回到 English OS 貼上 Report，讓網站驗證並保存已練與剩餘項目。

## 自動驗收結果

指定的八個情境已轉成 deterministic tests：

- A：sibling 未講完時，Action=`WAIT`，Coach speech 為空。
- B：niece 重要錯誤只做 Correction + Retry，結尾為 `Now try it again.`。
- C：Correction 後 Learner 只說 `Okay`，Action=`REQUEST_RETRY`。
- D：ancestor 沒有出現在可靠 Learner 原句時，Action=`ELICIT_TARGET`。
- E：`My ancestor is my father.` 判定 Target Usage 錯誤並要求 Retry。
- F：錯誤 Retry 保持同一 Current Word，不可 Advance。
- G：Final Challenge 片段回答選擇空白 `WAIT`。
- H：Final Challenge 未達數量時只能繼續，禁止 Wrap-up。

結果：**8/8 PASS**。

另有測試覆蓋 niece 的 article / be、sibling 的 singular / be、spouse 的 future / verb form，以及 server 對錯誤 `coachTurnAction` 的拒絕。

## 真人 Voice 驗收

自動測試只能驗證 Prompt、狀態選擇與回報規則。正式判定 Voice 行為前，仍需在 ChatGPT Project 語音模式逐一跑 A–H；每題單獨測，不必先做完整五字 Session。

驗收時記錄：Learner 原話、Coach 原話、預期 Action、實際 Action、PASS/FAIL。八題全數通過後再測完整 Session。
